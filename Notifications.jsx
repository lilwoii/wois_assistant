import React from "react";

export default function Notifications() {
  return (
    <div className="card">
      <div className="card-title">Notifications</div>
      <p className="text-muted">
        AI alerts, risk warnings, and Discord logs will appear here once the
        feed is wired in.
      </p>
    </div>
  );
}
